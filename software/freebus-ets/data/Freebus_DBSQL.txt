CREATE TABLE application_program (
   PROGRAM_ID BIGINT PRIMARY KEY,
   SYMBOL_ID BIGINT,
   MASK_ID BIGINT,
   PROGRAM_NAME TEXT(100),
   PROGRAM_VERSION TEXT(50),
   PROGRAM_VERSION_NUMBER INTEGER,
   LINKABLE INTEGER,
   DEVICE_TYPE BIGINT,
   PEI_TYPE INTEGER,
   ADDRESS_TAB_SIZE INTEGER,
   ASSOCTAB_ADDRESS BIGINT,
   ASSOCTAB_SIZE INTEGER,
   COMMSTAB_ADDRESS BIGINT,
   COMMSTAB_SIZE INTEGER,
   PROGRAM_SERIAL_NUMBER TEXT(50),
   MANUFACTURER_ID INTEGER,
   EEPROM_DATA LONGTEXT(5000),
   DATA_LENGTH BIGINT,
   S19_FILE TEXT(150),
   MAP_FILE TEXT(150),
   ASSEMBLER_ID BIGINT,
   HELP_FILE_NAME TEXT(100),
   CONTEXT_ID INTEGER,
   DYNAMIC_MANAGEMENT INTEGER,
   PROGRAM_TYPE INTEGER,
   RAM_SIZE BIGINT,
   ORIGINAL_MANUFACTURER_ID INTEGER,
   API_VERSION INTEGER,
   PROGRAM_STYLE INTEGER,
   IS_POLLING_MASTER INTEGER,
   NUMBER_OF_POLLING_GROUPS INTEGER,
   AllowedInSimpleEts INTEGER,
   MinEtsVersion INTEGER 
);

CREATE TABLE communication_object (
   PROGRAM_ID BIGINT,
   OBJECT_NAME TEXT(100),
   OBJECT_FUNCTION TEXT(100),
   OBJECT_READENABLED INTEGER,
   OBJECT_WRITEENABLED INTEGER,
   OBJECT_COMMENABLED INTEGER,
   OBJECT_TRANSENABLED INTEGER,
   OBJECT_DISPLAY_ORDER INTEGER,
   PARENT_PARAMETER_VALUE INTEGER,
   OBJECT_ID BIGINT,
   PARAMETER_ID BIGINT,
   OBJECT_NUMBER INTEGER,
   OBJECT_TYPE INTEGER,
   OBJECT_PRIORITY INTEGER,
   OBJECT_UPDATEENABLED INTEGER,
   OBJECT_UNIQUE_NUMBER INTEGER 
);

CREATE TABLE hw_product (
   PRODUCT_ID BIGINT PRIMARY KEY,
   MANUFACTURER_ID BIGINT,
   SYMBOL_ID INTEGER,
   PRODUCT_NAME TEXT(100),
   PRODUCT_VERSION_NUMBER INTEGER,
   COMPONENT_TYPE INTEGER,
   COMPONENT_ATTRIBUTES INTEGER,
   BUS_CURRENT INTEGER,
   PRODUCT_SERIAL_NUMBER TEXT(50),
   COMPONENT_TYPE_NUMBER INT,
   PRODUCT_PICTURE TEXT(100),
   BCU_TYPE_NUMBER INTEGER,
   PRODUCT_HANDLING INTEGER,
   PRODUCT_DLL TEXT(100),
   ORIGINAL_MANUFACTURER_ID INTEGER 
);

CREATE TABLE manufacturer (
   MANUFACTURER_ID INTEGER PRIMARY KEY,
   MANUFACTURER_NAME TEXT(100) 
);

CREATE TABLE mask (
   MASK_ID BIGINT PRIMARY KEY,
   MASK_VERSION INTEGER,
   USER_RAM_START BIGINT,
   USER_RAM_END BIGINT,
   USER_EEPROM_START BIGINT,
   USER_EEPROM_END BIGINT,
   RUN_ERROR_ADDRESS BIGINT,
   ADDRESS_TAB_ADDRESS BIGINT,
   ASSOCTABPTR_ADDRESS BIGINT,
   COMMSTABPTR_ADDRESS BIGINT,
   MANUFACTURER_DATA_ADDRESS BIGINT,
   MANUFACTURER_DATA_SIZE BIGINT,
   MANUFACTURER_ID_ADDRESS BIGINT,
   ROUTECNT_ADDRESS BIGINT,
   MANUFACTURER_ID_PROTECTED BIGINT,
   MASK_VERSION_NAME TEXT(50),
   MASK_EEPROM_DATA LONGTEXT(5000),
   MASK_DATA_LENGTH BIGINT,
   ADDRESS_TAB_LCS BIGINT,
   ASSOC_TAB_LCS BIGINT,
   APPLICATION_PROGRAM_LCS BIGINT,
   PEI_PROGRAM_LCS BIGINT,
   LOAD_CONTROL_ADDRESS BIGINT,
   RUN_CONTROL_ADDRESS BIGINT,
   PORT_ADDRESS_PROTECTED INTEGER,
   MEDIUM_TYPE_NUMBER INTEGER,
   BCU_TYPE_NUMBER INTEGER 
);

CREATE TABLE parameter (
   PROGRAM_ID BIGINT,
   PARAMETER_TYPE_ID BIGINT,
   PARAMETER_NUMBER INTEGER,
   PARAMETER_NAME TEXT(100),
   PARAMETER_LOW_ACCESS BIGINT,
   PARAMETER_HIGH_ACCESS BIGINT,
   PARAMETER_SIZE INTEGER,
   PARAMETER_DISPLAY_ORDER INTEGER,
   PARAMETER_ADDRESS BIGINT,
   PARAMETER_BITOFFSET BIGINT,
   PARAMETER_DESCRIPTION TEXT(200),
   PARAMETER_ID BIGINT PRIMARY KEY,
   PAR_PARAMETER_ID BIGINT,
   PARAMETER_DEFAULT_LONG BIGINT,
   PATCH_ALWAYS integer,
   ADDRESS_SPACE BIGINT 
);

CREATE TABLE parameter_list_of_values (
   PARAMETER_TYPE_ID BIGINT ,
   REAL_VALUE INTEGER,
   DISPLAYED_VALUE TEXT(200),
   DISPLAY_ORDER INTEGER,
   PARAMETER_VALUE_ID BIGINT PRIMARY KEY,
   BINARY_VALUE_LENGTH INTEGER 
)


CREATE TABLE parameter_type (
   PARAMETER_TYPE_ID BIGINT PRIMARY KEY,
   ATOMIC_TYPE_NUMBER BIGINT,
   PROGRAM_ID BIGINT,
   PARAMETER_TYPE_NAME TEXT(200),
   PARAMETER_TYPE_LOW_ACCESS BIGINT,
   PARAMETER_TYPE_HIGH_ACCESS BIGINT,
   PARAMETER_TYPE_SIZE BIGINT 
);

CREATE TABLE product_to_program (
   PROD2PROG_ID BIGINT PRIMARY KEY,
   PRODUCT_ID BIGINT,
   PROGRAM_ID BIGINT,
   PROD2PROG_STATUS_CODE INTEGER,
   REGISTRATION_NUMBER TEXT(100),
   REGISTRATION_YEAR TEXT(100),
   ORIGINAL_REGISTRATION_NUMBER TEXT(100),
   ORIGINAL_REGISTRATION_YEAR TEXT(100),
   REGISTRATION_TS TEXT(100) 
);


CREATE TABLE catalog_entry (
   CATALOG_ENTRY_ID BIGINT PRIMARY KEY,
   PRODUCT_ID BIGINT,
   MANUFACTURER_ID BIGINT,
   ORDER_NUMBER TEXT(100),
   ENTRY_NAME TEXT(200),
   ENTRY_WIDTH_IN_MODULES INTEGER,
   ENTRY_WIDTH_IN_MILLIMETERS INTEGER,
   DIN_FLAG boolean,
   PAGE_NUMBER INTEGER,
   DESIGNATION_TYPE INTEGER,
   ENTRY_STATUS_CODE INTEGER,
   REGISTRATION_TS TEXT(100)
);

